let pessoa = {
    nome:'rayane',
    idade:27,
    cor:'negra',
}

console.log(pessoa.nome);

delete pessoa.idade; // deletando idade da pessoa

console.log(pessoa.idade);

pessoa.cabelo = true; // validando cabelo da pessoa

console.log(pessoa.cabelo);